import { styled } from "styled-components";

export const LogOutMoadleTitle = styled.p`
    font-size: 32px;
    font-weight: 700;
`;

export const LogOutMoadlBtnFlex = styled.div`
    width: 100%;
    display: flex;
    gap: 20px;
    
    & > button{
        width: 50%;
    }
;`